# 未実装・必須改修・今後対応 一覧

> 現在のコードベース（Swift / SwiftUI / Firebase）で**実装済みだが完全でない機能**、**TODOコメントが残っている機能**、**本番リリース前に必ず対応が必要な項目**を洗い出した一覧です。

---

## 1. サブスクリプション・プレミアム機能

### 1-1. StoreKit 2 / RevenueCat の本番実装【最重要】 ~~→ 実装完了~~

| 項目 | 現状 | 必須対応 |
|------|------|----------|
| 購入処理 | ✅ `Product.purchase()` + `.verified` 検証 + `transaction.finish()` 実装済み | App Store Connect で実際のProduct IDに変更後、テスト購入で確認 |
| 購入復元 | ✅ `AppStore.sync()` + `Transaction.currentEntitlements` で復元済み | 機種変更時の動作確認 |
| サーバー検証 | なし（UserDefaultsのみ） | ~~任意~~ App Store Connect サーバー通知 or 独自APIでレシート検証（推奨） |
| 商品ID | `com.yourapp.removeads.monthly`（ダミー） | **App Store Connect で作成後に `SubscriptionConfig.removeAdsProductId` を変更** |
| エラーメッセージ | ~~「近日公開予定です」~~ ✅ 削除済み | 実際の購入フローに差し替え済み。価格は `product.displayPrice` で表示 |
| デバッグ挙動 | ✅ シミュレータでは StoreKit 2 API を使用、それ以外はモック | StoreKit Configuration File を設定すればシミュレータでも本番フロー |
| トランザクション監視 | ✅ `Transaction.updates` で自動監視開始 | 購入・更新・返金時に自動で `isSubscribed` を更新 |

**残作業**: App Store Connect でサブスクリプション商品作成 → Product ID を `SubscriptionConfig.removeAdsProductId` に設定 → サンドボックス購入テスト

**関連ファイル**: `SubscriptionManager.swift`, `SettingsView.swift`

### 1-2. プレミアム解除の本番対応

- 現状: `setSubscribed(false)` でUserDefaults上書きのみ
- 改修: **サーバー側でもサブスク状態を管理**し、クラウド同期を行う（機種変更時の整合性確保）

---

## 2. 広告（AdMob）

### 2-1. Google Mobile Ads SDK 導入【最重要】 ~~→ 実装完了~~

| 項目 | 現状 | 必須対応 |
|------|------|----------|
| SDK導入 | ~~未導入~~ ✅ 導入済み | SPMで `Google-Mobile-Ads-SDK` v10.x を追加（`#if canImport`で分岐） |
| 広告表示 | ~~グレー帯プレースホルダー~~ ✅ `GADBannerView` 実装済み | `GADBannerViewRepresentable`（SwiftUIラッパー）に差し替え済み |
| アプリID | ~~Info.plist未設定~~ ✅ 設定済み | `GADApplicationIdentifier` = `ca-app-pub-1810074247562384~7190264881` |
| 広告ユニットID | ~~テスト用ID~~ ✅ 本番ID設定済み | `ca-app-pub-1810074247562384/5573930888` |
| ATT対応 | ~~未実装~~ ✅ 実装済み | `NSUserTrackingUsageDescription` + `AppDelegate` で許可要求 |
| COPPA対応 | ~~未実装~~ ✅ 実装済み | `tag_for_child_directed_treatment: true` を `GADRequest` に設定 |
| 収益化 | 未開始 | AdMob審査・公開後の収益化フロー構築 |

**残作業**: SPMでv10.xにダウングレード後、ビルド確認。`GADMobileAds.sharedInstance().start` で初期化。

**関連ファイル**: `AdBannerView.swift`, `AppDelegate.swift`, `Info.plist`

### 2-2. 広告表示制御

- 現状: `isSubscribed` で非表示制御のみ
- 改修: **AdMobのロード失敗時のフォールバック**（エラー時にプレースホルダーを非表示にする等）

---

## 3. アフィリエイト機能

### 3-1. 楽天・アマゾンアフィリエイトID登録【最重要】

| 項目 | 現状 | 必須対応 |
|------|------|----------|
| 楽天アフィリエイトID | 空文字（通常検索URL） | 楽天アフィリエイト登録後、IDを `AffiliateConfig.affiliateIdRakuten` に設定 |
| Amazonアソシエイトタグ | 空文字（通常検索URL） | Amazonアソシエイト登録後、タグを `AffiliateConfig.associateTagAmazon` に設定 |
| リンク生成 | 動的生成済み | 特に改修不要（ID設定のみ） |

**関連ファイル**: `AffiliateManager.swift`

### 3-2. アフィリエイトリンクのクリック計測

- 現状: リンク生成のみ。クリック数・収益の追跡なし
- 改修: URLにトラッキングパラメータを付与、またはアプリ内ブラウザで遷移時の計測を追加

---

## 4. プッシュ通知（FCM）

### 4-1. FCMトークンの永続化【必須】 ~~→ 実装完了~~

| 項目 | 現状 | 必須対応 |
|------|------|----------|
| トークン取得 | ✅ `NotificationService.saveFCMTokenIfSignedIn()` で取得・Firestore保存済み | `try await` + `catch` でエラーハンドリング強化済み |
| トークン更新 | ✅ `AppDelegate.messaging(_:didReceiveRegistrationToken:)` でFirestore更新済み | リフレッシュ時に `fcm_token` を自動更新 |
| 通知配信 | なし | Cloud Functionsで「フォロー中ユーザーへの新着投稿通知」を実装 |

**関連ファイル**: `NotificationService.swift`, `AppDelegate.swift`

### 4-2. 通知内容の設計

- 未設計項目:
  - 通知の種類（新着投稿・いいね・フォローされた）
  - 通知の優先度・バッジ・サウンド設定
  - 通知タップ時の画面遷移先

---

## 5. セキュリティ・運用

### 5-1. Firestore Security Rules【最重要・未設定】

現状: Security Rules が未設定（もしくはデフォルトのまま）。本番公開前に必ず設定が必要。

```
推奨ルール（概要）:
- users: 自分のドキュメントのみ書き換え可。読み取りは全員可。
- posts: 作成は認証済みユーザー。自分の投稿のみ削除可。
- likes: 自分のUIDを含むドキュメントのみ作成・削除可。
- follows: 認証済みユーザーが作成可。自分の関係のみ削除可。
- reports: 認証済みユーザーが作成可。読み取りは管理者のみ。
```

**対応方法**: Firebase Console → Firestore Database → Rules タブで編集・デプロイ

### 5-2. App Check（推奨）

- 現状: 未導入
- 改修: Firebase App Check（App Attest / DeviceCheck）を導入し、不正APIアクセスを防止

---

## 6. データ・旧仕様移行

### 6-1. 単一子供 → 複数子供のデータ移行【必須】

| 項目 | 現状 | 必須対応 |
|------|------|----------|
| 旧仕様フィールド | `child_birthday`, `child_gender`（単一子供） | 既存ユーザーのデータを `children` 配列に移行 |
| 移行スクリプト | なし | Cloud Functionsまたはバッチ処理で一括移行 |
| 参照箇所 | `AuthViewModel` で新規登録時に旧フィールドを設定 | 新規ユーザーには `children` のみ設定。旧フィールドは非推奨化 |
| おすすめタブ | `user.child_birthday` を直接参照 | `user.children?.first?.birthday` を参照するように修正 |

**関連ファイル**: `AuthViewModel.swift`, `PostsViewModel.swift`, `Models.swift`

### 6-2. PostDraft の互換性デコード

- 現状: `id` フィールドがない旧下書きデータに対するカスタムデコードを実装済み
- 改修: **将来のバージョンアップ時に新フィールドを追加する場合**、同様の後方互換デコードが必要

---

## 7. エラーハンドリング・品質改善

### 7-1. `try?` の多用によるエラー隠蔽【必須改修】 ~~→ 実装完了~~

| 箇所 | 現状 | 対応内容 |
|------|------|----------|
| `toggleLike()` | ~~`try? await`~~ ✅ 改修済み | 楽観的UI更新（即時反映）→ エラー時にUIをロールバック＋`errorMessage` 表示 |
| `report()` | ~~`try? await`~~ ✅ 改修済み | `try await` + `catch` でエラーメッセージ表示。「すでに通報済み」の重複防止 |
| `deletePost()` | ~~`try? await`~~ ✅ 改修済み | Storage画像削除は失敗しても続行、サブコレクション・ドキュメント削除は `try await` で厳密に |
| `deleteAccount()` | Storage画像削除を `try? await` | 未改修（`AuthViewModel`側） |
| `enrichWithPosterInfo()` | `try? await` でユーザー取得 | 未改修（影響小） |

**改修方針**: ユーザーに影響の小さい処理（いいね・通報）は即時UI反映＋バックグラウンド再試行。重大な処理（削除）はエラーメッセージ表示。

### 7-2. アカウント削除の整合性

- 現状: Firestore削除 → Storage削除 → Auth削除の順で実行。途中で失敗してもロールバックなし
- 改修: **トランザクションまたはCloud Functions**でアトミックに削除。または、失敗時の補正処理を追加

---

## 8. 画像・メディア処理

### 8-1. 画像キャッシュの有効期限管理

- 現状: `ImageCacheService` はメモリ（200枚/100MB）＋ディスクキャッシュを実装。ただし**ディスクキャッシュの有効期限管理がない**
- 改修: ディスクキャッシュのクリーンアップ（一定期間経過 or 容量超過時の自動削除）

### 8-2. 下書き画像の不要ファイルクリーンアップ ~~→ 実装完了~~

- ~~現状: 下書き削除時にファイル削除の処理がない~~
- ✅ **改修済み**: `DraftManager` に以下を追加
  - `deleteDraftById(_:)` — 指定IDの下書き＋画像ファイルを完全削除（投稿成功後に使用）
  - `cleanupDraftImages(draftId:)` — 画像ファイルのみ削除
- `NewPostView.submitPost()` 成功後に `deleteDraftById()` を呼び出し、下書きデータと画像ファイルを両方削除
- **残作業**: アプリ起動時の孤児ファイル（UserDefaultsにない画像ファイル）の定期掃除

---

## 9. UI/UX改善・未実装機能

### 9-1. 投稿編集機能

- 現状: **投稿後の編集は完全に不可**
- 改修: 投稿者本人のみ、説明文・アイテム情報を編集可能にする（画像の差し替えは不要）

### 9-2. 通知・お知らせ画面

- 現状: 未実装
- 改修: いいねされた通知・フォローされた通知・運営からのお知らせを表示する画面を追加

### 9-3. フォロワー一覧の非表示対応

- 現状: フォロワー数は表示されるが「誰がフォローしているか」は非表示（セーフティ対策として設計通り）
- 確認: 設計通りだが、将来的に「承認制フォロー」に変更する可能性あり

### 9-4. 通報後の運用フロー

- 現状: 5回通報で自動非表示のみ。運営確認・復元フローなし
- 改修: **管理画面（Web）**を構築し、通報された投稿を運営が確認・復元・ユーザー停止できるようにする

### 9-5. 検索結果の保存・履歴

- 現状: 未実装
- 改修: よく使う検索条件の保存、検索履歴の表示

### 9-6. ローディング・空状態の改善

- 現状: いくつかの画面で空状態（Empty State）を実装済み
- 改修: 全画面で統一されたEmpty Stateデザインを適用。ネットワークエラー時のリトライUIを追加

---

## 10. テスト・品質保証

### 10-1. ユニットテスト【必須】

- 現状: **テストコードが完全に未実装**
- 必須テスト項目:
  - `AuthViewModel`: ログイン・新規登録・プロフィール保存の成功/失敗ケース
  - `PostsViewModel`: 投稿作成・いいね・通報・削除の成功/失敗ケース
  - `SubscriptionManager`: DEBUG/RELEASEでの購入フロー分岐
  - `BrandData`: ふりがな検索の正規化ロジック
  - `WeatherService`: APIレスポンスのパース・WMOコードマッピング

### 10-2. UIテスト（XCUITest）【推奨】

- 現状: 未実装
- 推奨項目: ログイン → 投稿 → いいね → ログアウトの一連フロー

### 10-3. ビルド設定・証明書

| 項目 | 現状 | 必須対応 |
|------|------|----------|
| コード署名 | 現状不明 | App Store配信用の証明書・Provisioning Profile設定 |
| Info.plist | AdMob ID未設定 | `GADApplicationIdentifier` 追加 |
| プライバシーマニフェスト | 未対応 | iOS 17+ の Privacy Manifest（Nutrition Label）対応 |
| ATS | 不明 | App Transport Security設定（Open-Meteo API等のhttp対応確認） |

---

## 11. インフラ・運用

### 11-1. Firebase環境分離

| 項目 | 現状 | 必須対応 |
|------|------|----------|
| プロジェクト | 1つのみ（おそらく） | 開発用・ステージング用・本番用の3環境構築 |
| GoogleService-Info.plist | 1つのみ | 環境ごとに異なるplistをXcode Schemeで切り替え |
| Analytics | 未確認 | Firebase Analyticsのイベント計測を追加（投稿数・いいね数等） |
| Crashlytics | 未確認 | Firebase Crashlytics導入でクラッシュ監視 |

### 11-2. バックアップ・監視

- Firestore・Storageの自動バックアップはFirebase側で管理
- Cloud Functionsによる日次バッチ処理（古い下書き削除・不要画像削除等）の構築が推奨

---

## 12. その他技術的負債

### 12-1. `AvatarIcons` の動的追加フロー

- 現状: `avatarImageNames` にハードコードで7種類を登録
- 改修: 運営側で新しいアバターを追加するたびにアプリ更新が必要。Firestoreに移行し動的取得を検討

### 12-2. ブランドマスターの運用

- 現状: `BrandData.swift` に350種類以上をハードコード
- 改修: 今後の追加・読み方修正はアプリ更新が必要。**Firestoreに移行**し、運営側が管理画面で追加・編集できるようにする

### 12-3. スタンプ機能の拡張

- 現状: Assets内9種類のスタンプのみ
- 改修: ユーザーがテキストスタンプを追加できる機能、または動的スタンプの追加

---

## 優先度まとめ

### ✅ 対応済み項目

| 項目 | 対応内容 |
|------|----------|
| AdMob SDK導入・設定 | `AdBannerView.swift`, `AppDelegate.swift`, `Info.plist` 完了。残: SPM v10.x ダウングレード後ビルド確認 |
| FCMトークン保存 | `NotificationService.swift`, `AppDelegate.swift` で `try await` + エラーハンドリング済み |
| いいね・通報・削除のエラーハンドリング | `PostsViewModel.toggleLike()`, `.report()`, `.deletePost()` で楽観的UI更新＋エラーロールバック |
| 下書き画像クリーンアップ | `DraftManager.deleteDraftById()` + `NewPostView` 投稿成功後削除 |

### 🔴 未対応項目（次の優先順位）

| 優先度 | 項目 | 理由 |
|--------|------|------|
| **P0（リリースブロッカー）** | **StoreKit 2実装** | DEBUGモックのままでは審査通過不可。`Product.purchase()` + サーバー検証 |
| **P0** | **Firestore Security Rules** | 未設定のまま公開するとデータ改ざんリスク |
| **P0** | **アフィリエイトID設定** | 楽天・AmazonのIDを `AffiliateConfig` に設定するだけ（外部登録必要） |
| **P0** | **コード署名・証明書・Provisioning Profile** | App Store審査には必須 |
| **P1（リリース後すぐ）** | **データ移行（単一子供→複数子供）** | 旧 `child_birthday` フィールドを `children` 配列に移行 |
| **P1** | **テストコード** | `AuthViewModel`, `PostsViewModel`, `WeatherService` のユニットテスト未実装 |
| **P1** | **Crashlytics・Analytics** | クラッシュ監視・行動計測の導入 |
| **P1** | **アカウント削除の整合性** | `AuthViewModel.deleteAccount()` の `try?` 改修、トランザクション化 |
| **P2（中期的）** | 通知画面・プッシュ配信（Cloud Functions） | いいね・フォロー通知の配信基盤 |
| **P2** | 投稿編集機能 | 投稿者本人が説明文・アイテムを編集 |
| **P2** | 管理画面（通報確認） | 5回通報で自動非表示だが、運営確認・復元フローなし |
| **P3（長期的）** | ブランドマスターFirestore移行、検索履歴、アバター動的追加、Privacy Manifest対応 |
