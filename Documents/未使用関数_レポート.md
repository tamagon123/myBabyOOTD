# 未使用関数・処理 レポート

**作成日**: 2026年6月7日  
**対象アプリ**: Nanikiru（乳児服装共有SNS）

---

## 🔴 未使用（削除または活用検討推奨）

### 1. `AffiliateManager.generateLinksForPost(items:)`
- **ファイル**: `Services/AffiliateManager.swift:177`
- **状態**: 定義されているが呼び出し箇所なし
- **説明**: 投稿のアイテムリストから全アフィリエイトリンクを一括生成する関数
- **推奨**: 現状は未使用だが、将来投稿詳細画面で全アイテム一括表示する場合に活用可能

```swift
func generateLinksForPost(items: [PostItem]) -> [(item: PostItem, links: [AffiliateLink])]
```

### 2. `CalendarSettings` 構造体
- **ファイル**: `Models/Models.swift:275`
- **状態**: 定義されているが直接インスタンス化されていない
- **説明**: カレンダー設定用のCodable構造体。実際には `users/{uid}.calendar_is_public` を直接Boolで管理
- **推奨**: 削除または、将来的にカレンダー設定が複雑化する場合のために残置

```swift
struct CalendarSettings: Codable {
    var is_public: Bool = false
}
```

---

## 🟡 開発・管理用（意図的にアプリ内から呼ばれない）

### 3. BrandService 移行用ユーティリティ
- **ファイル**: `Services/BrandService.swift`

| 関数名 | 行数 | 用途 |
|--------|------|------|
| `uploadDefaultBrands()` | 208 | ローカルのブランド配列をFirestoreに一括アップロード |
| `uploadDefaultShoppingPortals()` | 228 | デフォルトのショッピングポータルをFirestoreに一括アップロード |
| `createEmptyAffiliateConfig()` | 271 | config/affiliate ドキュメントを空状態で作成 |
| `setAffiliateLink(brandName:platform:url:)` | 293 | 個別ブランドのアフィリエイトリンクを手動登録 |

- **備考**: これらは**アプリ内では呼ばれない**が、管理ツールや開発時のデータ移行用に意図的に残置
- **推奨**: 削除せず残置（開発・運用に必要）

---

## 🟢 後方互換・ラッパー（使用箇所あり）

### 4. `CalendarViewModel` 後方互換メソッド
- **ファイル**: `ViewModels/CalendarViewModel.swift`

| 関数名 | 行数 | 状態 |
|--------|------|------|
| `fetchEntries(uid:around:)` | 196 | `fetchAllData` のエイリアス（CalendarViewで使用） |
| `fetchPosts(uid:around:)` | 199 | `fetchAllData` のエイリアス（CalendarViewで使用） |

- **備考**: これらは現在も `CalendarView.swift` から呼び出されている
- **推奨**: リファクタリング時に統合検討

---

## 📊 コード統計

### ViewModels/PostsViewModel.swift（一部未使用メソッド）
- `searchByDateAndRegion(date:regionCode:)` - ✅ 使用あり（CalendarViewから）
- `clearSearch()` - ✅ 使用あり（HomeViewから）
- `toggleLike(post:)` - ✅ 使用あり
- `report(post:)` - ✅ 使用あり
- `uploadPost(...)` - ✅ 使用あり
- `deletePost(_:)` - ✅ 使用あり（PostDetailView）
- `fetchLikedPosts()` - ✅ 使用あり

### Services/AffiliateManager.swift
- `generateLinks(brandName:category:size:preferredOnly:)` - ✅ 使用あり（AffiliateCardView, KeywordCardView）
- `generateLinksForPost(items:)` - 🔴 **未使用**

### Models/Models.swift
- `CalendarEntry` - ✅ 使用あり
- `CalendarSettings` - 🔴 **未使用（構造体定義のみ）**
- `AppNotification` - ✅ 使用あり
- `PostDraft` - ✅ 使用あり

---

## 📝 推奨アクション

### 即座に削除可能
1. `CalendarSettings` 構造体（使用箇所がない）

### 活用検討
1. `generateLinksForPost` - 投稿詳細画面の「全アイテムまとめて購入」機能で活用可能

### 残置推奨（開発・運用用途）
- `uploadDefaultBrands`
- `uploadDefaultShoppingPortals`
- `createEmptyAffiliateConfig`
- `setAffiliateLink`

---

**補足**: 未使用検出はgrep検索と静的解析に基づく。動的に呼び出される可能性（リフレクション等）は考慮していない。
